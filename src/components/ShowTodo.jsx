import React from 'react'

const ShowTodo = ({todo}) => {
    return (
        <div>
            <p>{todo.todo}</p>
            <div>
                <button></button>
                <button></button>
                <button></button>
            </div>
        </div>
    )
}

export default ShowTodo
